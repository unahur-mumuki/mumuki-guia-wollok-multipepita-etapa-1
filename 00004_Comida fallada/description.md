Ya tenemos a `pepita` que sabe comer distintas cosas ... el código de `pepita` acá lo escondemos, para no spoilear la resolución del ejercicio anterior. También están definidos `alpiste` y `mondongo`, podés probar en la consola.

Lo que **sí** se ve es la definición de un objeto `bigMac`.
Probar darle de comer un BigMac a Pepita. Va a dar un error ¿por qué? Arreglar la implementación de `bigMac` para transformarlo en un alimento posible para Pepita.