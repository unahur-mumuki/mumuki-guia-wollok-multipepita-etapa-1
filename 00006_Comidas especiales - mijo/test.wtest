describe "Group mijo"{
  var ave = pepita
  
	test "Mijo mojarse" {
	  mijo.mojarse()
	  ave.comer(mijo, 10)
		assert.equals(150, ave.energia())
	}
	
	test "Mijo secarse" {
	  mijo.secarse()
	  pepita.comer(ave, 10)
	  assert.equals(350, ave.energia())
	}
	
}